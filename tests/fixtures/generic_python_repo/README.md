# Generic CLI Fixture

Run `python3 scripts/cli.py --repo . --write` to create output in this repo.

Configuration keys:
- `repo_root`
- `write`

Reference files:
- `scripts/cli.py`
- `config.schema.json`
